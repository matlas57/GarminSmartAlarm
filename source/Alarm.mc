class Alarm {
    var earliestHour;
    var earliestMinute;
    var latestHour;
    var latestMinute;

    function initialize(eh, em, lh, lm) {
        earliestHour = eh;
        earliestMinute = em;
        latestHour = lh;
        latestMinute = lm;
    }


}